/**
 * 앱 라우트 경로
 */
export const ROUTES = {
  ROOT: '/',
  LANDING: '/landing',
  HOME: '/home',
  LOGIN: '/login',
  AUTH_CALLBACK: '/auth/callback',
  DASHBOARD: '/dashboard',
  PROFILE: '/profile',
  WORKSPACE_STUDIO: '/workspace/studio',
} as const;
